version: '3.8'

services:
  table-wars:
    build:
      context: .
      args:
        - NEXT_PUBLIC_SUPABASE_URL
        - NEXT_PUBLIC_SUPABASE_ANON_KEY
    ports:
      - "3000:80"
    restart: unless-stopped
